<!doctype html>
<html lang="en">
 <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap');
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3f4f6;
        }
        /* New custom CSS to enforce column and dropdown width */
        .inspector-column {
            width: 300px;
            min-width: 300px;
        }
        .inspector-select {
            width: 100%;
            min-width: 300px;
        }
        .status-select {
            width: 150px; /* Increased width for status dropdown */
            min-width: 150px;
        }
        
        /* Print-specific styles */
        @media print {
            @page {
                size: legal landscape;
                margin: 0.5cm;
            }
            body {
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
                background-color: #fff;
                font-family: 'Inter', sans-serif;
            }
            .no-print {
                display: none !important;
            }
            .main-print-container {
                width: 100%;
                max-width: 100%;
                margin: 0;
                padding: 0;
            }
            .header-print-section {
                display: flex;
                align-items: center;
                justify-content: space-between;
                margin-bottom: 0.5cm;
            }
            .header-info {
                display: flex;
                align-items: flex-start;
                gap: 10px;
            }
            .print-logo {
                width: 70px;
                height: auto;
                flex-shrink: 0;
            }
            .header-text-block p {
                margin: 0;
                line-height: 1.1;
                font-size: 8pt;
            }
            .report-titles {
                text-align: center;
                flex-grow: 1;
            }
            .report-title {
                font-size: 14pt;
                font-weight: bold;
                margin: 0;
            }
            .report-subtitle {
                font-size: 12pt;
                font-weight: bold;
                margin: 0;
            }
            .date-range-print {
                font-size: 10pt;
                font-style: italic;
                text-align: center;
                margin-top: 5px;
            }
            table {
                width: 100%;
                table-layout: fixed;
                border-collapse: collapse;
                
            }
            th, td {
                border: 1px solid #000;
                padding: 2px 4px;
                font-size: 7pt;
                text-align: left;
                vertical-align: top;
                word-wrap: break-word;
            }
            thead {
                background-color: #e5e7eb;
            }
            .actions-column {
                display: none !important;
            }
            .no-column { width: 2.5% !important; }
            .control-column { width: 8.5% !important; }
            .datetime-column { width: 10.5% !important; }
            .office-column { width: 9% !important; }
            .requested-by-column { width: 9% !important; }
            .disbursing-column { width: 9% !important; }
            .purpose-column { width: 9% !important; }
            .particulars-column { width: 10% !important; }
            .category-column { width: 6% !important; }
            .cost-column { width: 5.5% !important; }
            .inspector-column { width: 11% !important; }
            .status-column { width: 6% !important; }
            .days-column { width: 4% !important; }
        }
    </style>
  <style>body { box-sizing: border-box; }</style>
  <script src="https://cdn.tailwindcss.com/3.4.17" type="text/javascript"></script>
  <script src="/_sdk/data_sdk.js" type="text/javascript"></script>
  <script src="/_sdk/element_sdk.js" type="text/javascript"></script>
 </head>
 <body class="min-h-screen flex flex-col">
  <!-- Login Page -->
  <div id="loginPage" class="flex-1 flex items-center justify-center bg-gray-100 p-4" style="display: flex;">
   <div class="bg-white p-8 rounded-xl shadow-2xl max-w-lg w-full text-center">
    <div class="flex flex-col items-center mb-6">
     <div class="w-16 h-16 bg-blue-600 rounded-full flex items-center justify-center mb-4">
      <span class="text-white text-3xl">🔑</span>
     </div>
     <h1 class="text-3xl font-bold text-gray-900">Admin Login</h1>
     <p class="text-gray-500 mt-2">Access the inspection request dashboard</p>
    </div><!-- Login Form -->
    <form id="loginForm" class="space-y-6">
     <div>
      <label for="email" class="sr-only">Email</label> <input type="email" id="email" name="email" placeholder="Admin Email" required class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-200">
     </div>
     <div class="relative">
      <label for="password" class="sr-only">Password</label> <input type="password" id="password" name="password" placeholder="Password" required class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-200 pr-10"> <button type="button" id="togglePasswordVisibility" class="absolute inset-y-0 right-0 flex items-center px-4 text-gray-600"> <!-- Eye icon for show/hide password -->
       <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewbox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a. 0z" /> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
       </svg></button>
     </div><button type="submit" id="loginButton" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 px-6 rounded-xl transition duration-300 shadow-lg transform hover:scale-105"> Login </button>
    </form>
    <p id="loginMessage" class="text-red-500 mt-4 hidden"></p>
   </div>
  </div><!-- Dashboard Page -->
  <div id="dashboardPage" class="hidden flex-1 flex-col">
   <!-- Header -->
   <header class="no-print bg-white shadow-lg p-4 flex justify-between items-center sticky top-0 z-50">
    <div class="flex items-center space-x-4">
     <div class="w-10 h-10 bg-blue-600 rounded-full flex items-center justify-center">
      <span class="text-white text-xl">📊</span>
     </div>
     <h1 class="text-2xl font-bold text-gray-900">Admin Dashboard</h1>
    </div>
    <div class="flex items-center space-x-4">
     <span id="welcomeMessage" class="text-gray-600 text-sm">Welcome, Admin</span> <button id="logoutButton" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg transition-colors"> Logout </button>
    </div>
   </header><!-- Main Content -->
   <main class="flex-1 p-8">
    <div class="max-w-7xl mx-auto main-print-container">
     <div class="flex items-start mb-6 header-print-section">
      <div class="flex items-center header-info">
       <img src="https://www.pup.edu.ph/resources/images/logo200.png" alt="PUP Logo" class="w-20 h-auto mr-4 print-logo" onerror="this.src='https://placehold.co/80x80/FFFFFF/000000?text=LOGO';">
       <div class="text-left header-text-block">
        <p class="text-xs text-gray-500">Republic of the Philippines</p>
        <p class="text-sm font-semibold text-gray-800">Polytechnic University of the Philippines</p>
        <p class="text-sm font-semibold text-gray-800">OFFICE OF EXECUTIVE VICE PRESIDENT</p>
        <p class="text-sm font-semibold text-gray-800">Inspection Management Office</p>
       </div>
      </div>
     </div>
     <h2 class="text-3xl font-bold text-gray-900 text-center report-title">INSPECTION MONITORING REPORT</h2>
     <h3 class="text-xl font-bold text-gray-900 text-center report-subtitle">OPERATIONS PURCHASE</h3>
     <div class="text-center text-sm font-semibold text-gray-600 mb-6 flex justify-center items-center relative">
      <span id="dateDisplay" class="text-center italic cursor-pointer">AS OF: </span>
      <div id="dateInputs" class="flex justify-center space-x-4 mt-2 hidden absolute top-full left-1/2 -translate-x-1/2 bg-white p-4 border border-gray-300 rounded-lg shadow-md z-10">
       <input type="text" id="startDate" class="px-2 py-1 border border-gray-300 rounded-md w-36" placeholder="Month Day"> <span>-</span> <input type="text" id="endDate" class="px-2 py-1 border border-gray-300 rounded-md w-36" placeholder="Month Day, YYYY">
      </div>
     </div>
     <div class="no-print flex justify-end mb-4">
      <button id="exportCsvBtn" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-lg transition-colors shadow-md font-semibold"> 📥 Export to CSV </button>
     </div>
     <div class="bg-white rounded-xl shadow-lg overflow-hidden">
      <div class="overflow-x-auto">
       <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
         <tr>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider no-column">No.</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider control-column">Control No.</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider datetime-column">Date and Time Requested</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider datetime-column">Date and Time Inspected</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider office-column">Office/Department</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider requested-by-column">Requested By</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider disbursing-column">Disbursing Officer</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider purpose-column">Purpose</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider particulars-column">Particulars</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider category-column">Category</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider cost-column">Cost</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider inspector-column">Inspected by</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider status-column">Status</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider days-column">No. of Days</th>
          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider actions-column">Actions</th>
         </tr>
        </thead>
        <tbody id="requestsTableBody" class="bg-white divide-y divide-gray-200">
         <!-- Data will be loaded here -->
        </tbody>
       </table>
      </div>
     </div>
    </div>
   </main>
  </div><!-- Modal for viewing form details -->
  <div id="detailsModal" class="fixed inset-0 bg-black bg-opacity-75 flex items-center justify-center z-[9999]" style="display: none;">
   <div class="bg-white rounded-2xl shadow-2xl max-w-4xl mx-4 max-h-[90vh] overflow-y-auto">
    <div class="p-8">
     <div class="flex justify-between items-center pb-4 border-b border-gray-200 mb-4">
      <h3 class="text-2xl font-bold text-gray-900">Request Details</h3><button id="closeDetailsModal" class="text-gray-500 hover:text-gray-900 transition-colors">
       <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewbox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
       </svg></button>
     </div>
     <div id="modalContent">
      <!-- Formatted form content will be injected here -->
     </div>
    </div>
   </div>
  </div><!-- Delete Confirmation Modal -->
  <div id="deleteModal" class="fixed inset-0 bg-black bg-opacity-75 flex items-center justify-center z-[9999]" style="display: none;">
   <div class="bg-white rounded-lg p-6 shadow-xl max-w-sm mx-auto">
    <div class="flex items-center space-x-4 mb-4">
     <span class="text-red-500 text-2xl">🗑️</span>
     <h3 class="text-lg font-bold">Confirm Deletion</h3>
    </div>
    <p class="text-sm text-gray-700">Are you sure you want to delete this request? This action cannot be undone.</p>
    <div class="flex justify-end mt-6 space-x-4">
     <button id="cancelDeleteButton" class="bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold px-4 py-2 rounded-lg transition-colors"> Cancel </button> <button id="confirmDeleteButton" class="bg-red-600 hover:bg-red-700 text-white font-semibold px-4 py-2 rounded-lg transition-colors"> Delete </button>
    </div>
   </div>
  </div><!-- Modal for Custom Inspector Input -->
  <div id="customInspectorModal" class="fixed inset-0 bg-black bg-opacity-75 flex items-center justify-center z-[9999]" style="display: none;">
   <div class="bg-white rounded-lg p-6 shadow-xl max-w-sm mx-auto">
    <div class="flex items-center space-x-4 mb-4">
     <span class="text-blue-500 text-2xl">✏️</span>
     <h3 class="text-lg font-bold">Add Custom Inspector</h3>
    </div><input type="text" id="customInspectorInput" placeholder="Enter inspector name" class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 mb-4">
    <div class="flex justify-end space-x-4">
     <button id="cancelCustomInspector" class="bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold px-4 py-2 rounded-lg transition-colors"> Cancel </button> <button id="confirmCustomInspector" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-4 py-2 rounded-lg transition-colors"> Add </button>
    </div>
   </div>
  </div><!-- Modal for Firebase Security Rules Error -->
  <div id="firestoreRulesErrorModal" class="fixed inset-0 bg-black bg-opacity-75 flex items-center justify-center z-[9999]" style="display: none;">
   <div class="bg-white rounded-lg p-6 shadow-xl max-w-sm mx-auto">
    <div class="flex items-center space-x-4 mb-4">
     <span class="text-red-500 text-2xl">⚠️</span>
     <h3 class="text-lg font-bold">Firestore Rules Error</h3>
    </div>
    <p class="text-sm text-gray-700">The application lacks the necessary permissions to read and update data from Firestore. Please ensure your security rules are correctly configured. <br><br>
      **To fix this:** Go to your <a href="https://console.firebase.google.com/project/inspect-128d0/firestore" target="_blank" class="text-blue-500 underline">Firestore Rules</a> in the Firebase Console and publish the provided security rules.</p>
    <div class="flex justify-end mt-6">
     <button onclick="this.closest('.fixed').remove()" class="bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold px-4 py-2 rounded-lg transition-colors"> OK </button>
    </div>
   </div>
  </div>
  <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/12.9.0/firebase-app.js";
        import { getAuth, signInWithEmailAndPassword, signOut, onAuthStateChanged, setPersistence, browserLocalPersistence } from "https://www.gstatic.com/firebasejs/12.9.0/firebase-auth.js";
        import { getFirestore, collection, onSnapshot, doc, updateDoc, deleteDoc } from "https://www.gstatic.com/firebasejs/12.9.0/firebase-firestore.js";
        import { getAnalytics } from "https://www.gstatic.com/firebasejs/12.9.0/firebase-analytics.js";

        // Firebase configuration for inspection-322dc project
        const firebaseConfig = {
          apiKey: "AIzaSyAY5wW9N1alNSWjSK5coP23UfOLax4lcK8",
          authDomain: "inspection-322dc.firebaseapp.com",
          projectId: "inspection-322dc",
          storageBucket: "inspection-322dc.firebasestorage.app",
          messagingSenderId: "354261059462",
          appId: "1:354261059462:web:b235fc76b155bd80c6ce07",
          measurementId: "G-6VEDCC3C7N"
        };
        
        // Initialize Firebase
        const app = initializeApp(firebaseConfig);
        const analytics = getAnalytics(app);
        
        const auth = getAuth(app);
        const db = getFirestore(app);
        
        // Enable persistent login using Firebase (data stored in Firebase, not localStorage)
        await setPersistence(auth, browserLocalPersistence);
        
        // IMPORTANT: Update these credentials to match the user you create in Firebase Authentication.
        const ADMIN_EMAIL = "imoadmin@pup.edu.ph";
        const ADMIN_PASSWORD = "admin2025**";

        // State for custom inspector modal
        let pendingInspectorUpdate = null;

        // Initialize Firebase
        function initializeFirebase() {
            const analytics = getAnalytics(app);

            // Auth state listener
            onAuthStateChanged(auth, (user) => {
                if (user) {
                    if (user.email === ADMIN_EMAIL) {
                        document.getElementById('welcomeMessage').textContent = `Welcome, ${user.email}`;
                        showDashboard();
                        loadRequests();
                    } else {
                        signOut(auth);
                        showLoginError("You do not have administrative access.");
                    }
                } else {
                    showLogin();
                }
            });
        }
        
        // UI State Management
        function showLogin() {
            document.getElementById('loginPage').style.display = 'flex';
            document.getElementById('dashboardPage').style.display = 'none';
        }
        
        function showDashboard() {
            document.getElementById('loginPage').style.display = 'none';
            document.getElementById('dashboardPage').style.display = 'flex';
        }
        
        function showLoginError(message) {
            const loginMessage = document.getElementById('loginMessage');
            loginMessage.textContent = message;
            loginMessage.classList.remove('hidden');
        }
        
        function clearLoginError() {
            const loginMessage = document.getElementById('loginMessage');
            loginMessage.textContent = '';
            loginMessage.classList.add('hidden');
        }

        // Firebase Auth handlers
        document.getElementById('loginForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;
            
            clearLoginError();

            try {
                await signInWithEmailAndPassword(auth, email, password);
            } catch (error) {
                if (error.code === 'auth/user-not-found' || error.code === 'auth/wrong-password') {
                     showLoginError("Invalid email or password.");
                } else {
                     showLoginError("Login failed: " + error.message);
                }
            }
        });
        
        document.getElementById('logoutButton').addEventListener('click', async () => {
            await signOut(auth);
            showLogin();
        });
        
        // Toggle password visibility
        document.getElementById('togglePasswordVisibility').addEventListener('click', () => {
            const passwordInput = document.getElementById('password');
            const toggleButton = document.getElementById('togglePasswordVisibility');
            const isPassword = passwordInput.type === 'password';

            if (isPassword) {
                passwordInput.type = 'text';
                toggleButton.innerHTML = `
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.542-7 .473-1.545 1.054-3.037 1.748-4.428m11.282-1.34A10.05 10.05 0 0112 5c-4.478 0-8.268 2.943-9.542 7-.473 1.545-1.054 3.037-1.748 4.428m11.282-1.34L18 18m-4.125-6.125L6 6m7.875 6.125L18 18M6 6l12 12" />
                    </svg>
                `;
            } else {
                passwordInput.type = 'password';
                toggleButton.innerHTML = `
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a. 0z" />
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                    </svg>
                `;
            }
        });
        
        function updateDateDisplay() {
            const startDateInput = document.getElementById('startDate').value;
            const endDateInput = document.getElementById('endDate').value;
            const dateDisplay = document.getElementById('dateDisplay');
            
            const monthNames = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
            
            let start = '';
            if (startDateInput) {
                const parts = startDateInput.split(' ');
                if (parts.length >= 2) {
                    start = `${parts[0]} ${parts[1]}`;
                }
            }

            let end = '';
            let year = '';
            if (endDateInput) {
                const parts = endDateInput.split(' ');
                if (parts.length >= 2) {
                    end = `${parts[0]} ${parts[1].replace(',', '')}`;
                }
                if (parts.length > 2) {
                    year = parts[2];
                }
            }

            if (start && end && year) {
                dateDisplay.innerHTML = `<i>AS OF: ${start} - ${end}, ${year}</i>`;
            } else {
                const today = new Date();
                dateDisplay.innerHTML = `<i>AS OF: ${monthNames[today.getMonth()]} ${today.getDate()} - ${monthNames[today.getMonth()]} ${today.getDate()}, ${today.getFullYear()}</i>`;
            }
        }
        
        // Firestore Data Fetching
        function loadRequests() {
            const tableBody = document.getElementById('requestsTableBody');
            const requestsCollection = collection(db, `/artifacts/${firebaseConfig.projectId}/public/data/inspectionForms`);
            
            // Listen for real-time updates
            onSnapshot(requestsCollection, (snapshot) => {
                const filteredDocs = snapshot.docs.filter(doc => {
                    const data = doc.data();
                    const requestDate = new Date(data.timestamp);
                    const startDateInput = document.getElementById('startDate').value;
                    const endDateInput = document.getElementById('endDate').value;
                    
                    let isWithinRange = true;
                    if (startDateInput && endDateInput) {
                        const startParts = startDateInput.split(' ');
                        const endParts = endDateInput.split(' ');
                        
                        const startMonth = startParts[0];
                        const startDay = parseInt(startParts[1]);
                        const endMonth = endParts[0];
                        const endDay = parseInt(endParts[1].replace(',', ''));
                        const year = endParts.length > 2 ? parseInt(endParts[2]) : new Date().getFullYear();
                        
                        const startDate = new Date(`${startMonth} ${startDay}, ${year}`);
                        const endDate = new Date(`${endMonth} ${endDay}, ${year}`);

                        // Set time to end of day to include all requests from that day
                        endDate.setHours(23, 59, 59, 999);
                        
                        isWithinRange = requestDate >= startDate && requestDate <= endDate;
                    }
                    return isWithinRange;
                });

                tableBody.innerHTML = ''; // Clear existing data
                if (filteredDocs.length === 0) {
                     tableBody.innerHTML = `<tr><td colspan="15" class="px-6 py-4 text-center text-gray-500">No requests found for the selected date range.</td></tr>`;
                     return;
                }

                filteredDocs.forEach((doc, index) => {
                    const data = doc.data();
                    const docId = doc.id;
                    let cost = 'N/A';
                    if (data.totalAmount !== undefined && typeof data.totalAmount === 'number') {
                        cost = data.totalAmount.toFixed(2);
                    } else if (data.totalAmount !== undefined && typeof data.totalAmount === 'string') {
                        const parsedAmount = parseFloat(data.totalAmount);
                        if (!isNaN(parsedAmount)) {
                            cost = parsedAmount.toFixed(2);
                        }
                    }
                    const inspectors = [
                      "Dir. Ferdinand O. Natividad",
                      "Ch. Jonathan C. Manarang",
                      "Ins. Carmelo T. Osorio Jr.",
                      "Ins. Ara Diether Lei Y. De Chavez"
                    ];
                    let inspectedByOptions = inspectors.map(inspector =>
                        `<option value="${inspector}" ${data.inspectedBy === inspector ? 'selected' : ''}>${inspector}</option>`
                    ).join('');
                    
                    // Check if current inspectedBy is not in the list (custom entry)
                    if (data.inspectedBy && !inspectors.includes(data.inspectedBy)) {
                        inspectedByOptions += `<option value="${data.inspectedBy}" selected>${data.inspectedBy}</option>`;
                    }
                    inspectedByOptions += `<option value="__custom__">Custom Inspector...</option>`;

                    const statusOptions = ["Pending", "Complied", "Not Complied"];
                    let statusOptionsHtml = statusOptions.map(status =>
                        `<option value="${status}" ${data.status === status ? 'selected' : ''}>${status}</option>`
                    ).join('');

                    let statusColorClass = 'text-gray-500'; // Default color
                    if (data.status === 'Pending') {
                        statusColorClass = 'text-yellow-500';
                    } else if (data.status === 'Complied') {
                        statusColorClass = 'text-green-600';
                    } else if (data.status === 'Not Complied') {
                        statusColorClass = 'text-red-500';
                    }

                    // Calculate days since request
                    let days = 'N/A';
                    if (data.timestamp) {
                        const requestDate = new Date(data.timestamp);
                        const inspectedDate = data.inspectionTime ? new Date(data.inspectionTime) : null;
                        
                        let startDate;
                        let endDate;

                        if (inspectedDate && (data.status === 'Complied' || data.status === 'Not Complied')) {
                            startDate = new Date(requestDate.getFullYear(), requestDate.getMonth(), requestDate.getDate());
                            endDate = new Date(inspectedDate.getFullYear(), inspectedDate.getMonth(), inspectedDate.getDate());
                        } else {
                            startDate = new Date(requestDate.getFullYear(), requestDate.getMonth(), requestDate.getDate());
                            endDate = new Date();
                        }
                        
                        const diffTime = Math.abs(endDate - startDate);
                        const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
                        days = diffDays > 0 ? diffDays : 1;
                    }

                    const row = `
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 no-column">${index + 1}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 control-column">${data.controlNumber || 'N/A'}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 datetime-column">${data.timestamp || 'N/A'}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 datetime-column editable-field" contenteditable="true" data-id="${docId}" data-field="inspectionTime">${data.inspectionTime || 'Click to add...'}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 office-column">${data.officeDepartment || 'N/A'}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 requested-by-column">${data.requestedBy || 'N/A'}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 disbursing-column">${data.disbursingOfficer || 'N/A'}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 purpose-column">${data.purpose || 'N/A'}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 particulars-column">${data.particulars || 'N/A'}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 category-column">${data.category || 'N/A'}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 cost-column">${cost}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 inspector-column">
                                <select data-id="${docId}" data-field="inspectedBy" class="inspector-select bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5">
                                    <option value="">Select Inspector</option>
                                    ${inspectedByOptions}
                                </select>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm status-column">
                                <select data-id="${docId}" data-field="status" class="status-select ${statusColorClass} bg-gray-50 border border-gray-300 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5">
                                    ${statusOptionsHtml}
                                </select>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 days-column">${days}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium actions-column">
                                <button data-id="${docId}" data-days="${days}" class="view-btn text-blue-600 hover:text-blue-900 mr-4">View</button>
                                <button data-id="${docId}" class="delete-btn text-red-600 hover:text-red-900">Delete</button>
                            </td>
                        </tr>
                    `;
                    tableBody.innerHTML += row;
                });
                
                // Add event listeners to the new view buttons
                document.querySelectorAll('.view-btn').forEach(button => {
                    button.addEventListener('click', (e) => {
                        const requestId = e.target.getAttribute('data-id');
                        const docData = snapshot.docs.find(d => d.id === requestId).data();
                        const days = e.target.getAttribute('data-days'); // Retrieve days directly from the button's data attribute
                        showDetailsModal(docData, days);
                    });
                });

                 // Add event listeners for the editable fields
                document.querySelectorAll('.editable-field').forEach(cell => {
                    cell.addEventListener('blur', (e) => {
                        const id = e.target.getAttribute('data-id');
                        const field = e.target.getAttribute('data-field');
                        const value = e.target.textContent;
                        updateRequest(id, field, value);
                    });
                });
                
                document.querySelectorAll('.inspector-select').forEach(select => {
                    select.addEventListener('change', (e) => {
                        const id = e.target.getAttribute('data-id');
                        const field = e.target.getAttribute('data-field');
                        const value = e.target.value;
                        
                        if (value === '__custom__') {
                            // Show custom inspector modal
                            pendingInspectorUpdate = { id, field };
                            document.getElementById('customInspectorInput').value = '';
                            document.getElementById('customInspectorModal').style.display = 'flex';
                            document.getElementById('customInspectorInput').focus();
                        } else if (value !== '') {
                            updateRequest(id, field, value);
                        }
                    });
                });

                 document.querySelectorAll('.status-select').forEach(select => {
                    select.addEventListener('change', (e) => {
                        const id = e.target.getAttribute('data-id');
                        const field = e.target.getAttribute('data-field');
                        const value = e.target.value;
                        updateRequest(id, field, value);
                    });
                });

                 document.querySelectorAll('.delete-btn').forEach(button => {
                    button.addEventListener('click', (e) => {
                        const requestId = e.target.getAttribute('data-id');
                        document.getElementById('deleteModal').style.display = 'flex';
                        document.getElementById('confirmDeleteButton').onclick = () => deleteRequest(requestId);
                    });
                });

            }, (error) => {
                console.error("Firestore read error:", error);
                if (error.code === 'permission-denied') {
                    document.getElementById('firestoreRulesErrorModal').style.display = 'flex';
                }
            });
        }
        
        // Function to update Firestore document
        async function updateRequest(id, field, value) {
            const docRef = doc(db, `/artifacts/${firebaseConfig.projectId}/public/data/inspectionForms`, id);
            await updateDoc(docRef, {
                [field]: value
            });
        }
        
        // Function to delete Firestore document
        async function deleteRequest(id) {
            const docRef = doc(db, `/artifacts/${firebaseConfig.projectId}/public/data/inspectionForms`, id);
            try {
                await deleteDoc(docRef);
                document.getElementById('deleteModal').style.display = 'none';
            } catch (error) {
                console.error("Error deleting document: ", error);
                document.getElementById('deleteModal').style.display = 'none';
                let messageBox = document.createElement('div');
                messageBox.innerHTML = `
                    <div class="fixed inset-0 bg-black bg-opacity-75 flex items-center justify-center z-[9999]">
                        <div class="bg-white rounded-lg p-6 shadow-xl max-w-sm mx-auto">
                            <div class="flex items-center space-x-4 mb-4">
                                <span class="text-red-500 text-2xl">⚠️</span>
                                <h3 class="text-lg font-bold">Deletion Failed</h3>
                            </div>
                            <p class="text-sm text-gray-700">An error occurred while trying to delete the request. Please check your permissions and try again.</p>
                            <div class="flex justify-end mt-6">
                                <button onclick="this.closest('.fixed').remove()" class="bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold px-4 py-2 rounded-lg transition-colors">
                                    OK
                                </button>
                            </div>
                        </div>
                    </div>
                `;
                document.body.appendChild(messageBox);
            }
        }
        
        function showDetailsModal(data, days) {
            const modal = document.getElementById('detailsModal');
            const modalContent = document.getElementById('modalContent');
            
            const docReqsHtml = (data.documentaryRequirement || []).map(req => 
                `<li class="list-disc ml-6">
                     ${req === 'salesInvoice' ? 'Sales Invoice/Official Receipt' : ''}
                     ${req === 'approvedPurchase' ? 'Approved Purchase Request' : ''}
                     ${req === 'specialOrder' ? 'Special Order/Line Item Budget' : ''}
                     ${req === 'proofSurrender' ? 'Proof of Surrendered Waste' : ''}
                     ${req === 'endorsementLetter' ? 'Endorsement Letter' : ''}
                     ${req === 'others' ? `Others: ${data.othersText}` : ''}
                </li>`
            ).join('');
            let cost = 'N/A';
            if (data.totalAmount !== undefined && typeof data.totalAmount === 'number') {
                cost = data.totalAmount.toFixed(2);
            } else if (data.totalAmount !== undefined && typeof data.totalAmount === 'string') {
                const parsedAmount = parseFloat(data.totalAmount);
                if (!isNaN(parsedAmount)) {
                    cost = parsedAmount.toFixed(2);
                }
            }
            modalContent.innerHTML = `
                <div class="space-y-6 text-gray-700">
                    <h4 class="text-lg font-bold">Request Details</h4>
                    <p><strong>Control No.:</strong> ${data.controlNumber}</p>
                    <p><strong>Date and Time Requested:</strong> ${data.timestamp}</p>
                    <p><strong>Date and Time Inspected:</strong> ${data.inspectionTime || 'N/A'}</p>
                    <p><strong>Requested By:</strong> ${data.requestedBy}</p>
                    <p><strong>Office/Department:</strong> ${data.officeDepartment}</p>
                    <p><strong>Disbursing Officer:</strong> ${data.disbursingOfficer}</p>
                    <p><strong>Purpose:</strong> ${data.purpose}</p>
                    <p><strong>Particulars:</strong> ${data.particulars}</p>
                    <p><strong>Category:</strong> ${data.category}</p>
                    <p><strong>Cost:</strong> ${cost}</p>
                    <p><strong>Inspected by:</strong> ${data.inspectedBy || 'N/A'}</p>
                    <p><strong>Status:</strong> ${data.status || 'N/A'}</p>
                    <p><strong>No. of Days:</strong> ${days || 'N/A'}</p>
                    <h4 class="text-lg font-bold pt-4">Documentary Requirements</h4>
                    <ul class="space-y-1">
                        ${docReqsHtml}
                    </ul>
                    <div class="flex items-center space-x-2 pt-4">
                        <p class="font-bold">Remarks:</p>
                        <p class="editable-field-modal text-gray-700" contenteditable="true" data-id="${data.id}" data-field="remarks">${data.remarks || 'Click to add...'}</p>
                    </div>
                </div>
            `;
            
            modal.style.display = 'flex';
        }
        
        document.getElementById('closeDetailsModal').addEventListener('click', () => {
            document.getElementById('detailsModal').style.display = 'none';
        });
        
        document.getElementById('cancelDeleteButton').addEventListener('click', () => {
            document.getElementById('deleteModal').style.display = 'none';
        });

        // Custom Inspector Modal Handlers
        document.getElementById('confirmCustomInspector').addEventListener('click', () => {
            const customName = document.getElementById('customInspectorInput').value.trim();
            if (customName && pendingInspectorUpdate) {
                updateRequest(pendingInspectorUpdate.id, pendingInspectorUpdate.field, customName);
                document.getElementById('customInspectorModal').style.display = 'none';
                pendingInspectorUpdate = null;
            }
        });

        document.getElementById('cancelCustomInspector').addEventListener('click', () => {
            document.getElementById('customInspectorModal').style.display = 'none';
            pendingInspectorUpdate = null;
        });

        document.getElementById('customInspectorInput').addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                document.getElementById('confirmCustomInspector').click();
            }
        });
        
        document.getElementById('dateDisplay').addEventListener('click', () => {
            const dateInputs = document.getElementById('dateInputs');
            dateInputs.classList.toggle('hidden');
        });
        
        document.getElementById('exportCsvBtn').addEventListener('click', () => {
            let csv = '';
            
            // Add header info
            const headerInfo = [
                'PUP Logo URL: https://www.pup.edu.ph/resources/images/logo200.png',
                'Republic of the Philippines',
                'Polytechnic University of the Philippines',
                'OFFICE OF EXECUTIVE VICE PRESIDENT',
                'Inspection Management Office',
                '', // Empty line for spacing
                'INSPECTION MONITORING REPORT',
                'OPERATIONS PURCHASE',
                document.getElementById('dateDisplay').textContent,
                '' // Empty line for spacing
            ];
            csv += headerInfo.join('\n') + '\n';

            // Add table headers
            const headers = Array.from(document.querySelectorAll('thead th')).map(th => th.textContent.trim());
            csv += headers.join(',') + '\n';
            
            // Add table data
            const rows = document.querySelectorAll('#requestsTableBody tr');
            rows.forEach(row => {
                const rowData = [];
                row.querySelectorAll('td').forEach(cell => {
                    let cellText = '';
                    if (cell.querySelector('select')) {
                        cellText = cell.querySelector('select').value;
                    } else if (cell.querySelector('input')) {
                        cellText = cell.querySelector('input').value;
                    } else {
                        cellText = cell.textContent.trim();
                    }
                    // Sanitize data with quotes for CSV
                    if (cellText.includes(',')) {
                        cellText = `"${cellText}"`;
                    }
                    rowData.push(cellText);
                });
                csv += rowData.join(',') + '\n';
            });
            
            const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' });
            const url = URL.createObjectURL(blob);
            const link = document.createElement('a');
            link.setAttribute('href', url);
            link.setAttribute('download', 'inspection_report.csv');
            link.style.visibility = 'hidden';
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        });

        // Initialize
        initializeFirebase();
    </script>
 <script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'9d26dafa4313fee1',t:'MTc3MTg1MTA5NC4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
